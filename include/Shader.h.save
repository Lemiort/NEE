#ifndef SHADER_H
#define SHADER_H
#include <GL/glew.h>

class Shader
{
    public:
        enum ShaderType {Vertex, Geometry, Fragmnet};
        Shader();
        virtual ~Shader();
        void Init();//собирает и компилирует шейдер
        void AddShader(char* source, ShaderType type);
        GLuint shaderProgramID;
        void Use();
        GLuint GetAttribLocation(char* Attribute);
        GLuint GetUniformLocation(char* Attribute);
    protected:
    private:
        GLuint V
        bool initialized;
};

#endif // SHADER_H
